# FTP
is an legacy application and a protocol for transferring files and directories between computers over a data network, runs on top of a **TCP/IP** protocol,the files and folders can be transferred to either direction between the two connected computers. 

# About Security 
The file transfer protocol was not designed to be secure,also does not work well over firewalls and address translation, which are very common in today's networks.

